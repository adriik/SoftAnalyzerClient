<Window x:Class="SoftAnalyzerClient.Szczegoly"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SoftAnalyzerClient"
        mc:Ignorable="d"
        Title="Szczegoly" Height="450" Width="800">
    <Grid Margin="10,10,10,10" x:Name="gridziok">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*"/>
            <ColumnDefinition Width="1*"/>
        </Grid.ColumnDefinitions>
        <DataGrid AutoGenerateColumns="False" Grid.Column="0"
          Height="auto" 
          HorizontalAlignment="Left" 
          Name="dataGrid1" 
          VerticalAlignment="Top" 
          Width="auto"
          IsReadOnly="True" 
          SelectionChanged="DataGrid1_SelectionChanged"
          SelectionMode="Single">
            <DataGrid.Columns >
                <DataGridTextColumn Binding="{Binding Path=nazwa}" MinWidth="50" />
                <DataGridTextColumn Binding="{Binding Path=waga}" MinWidth="50" />
                <DataGridTextColumn Binding="{Binding Path=podobienstwo}" MinWidth="50" />
            </DataGrid.Columns>
        </DataGrid>
     
        
    </Grid>
</Window>
